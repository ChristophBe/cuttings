---
description: Use the cuttings CLI to create isolated git worktrees for independent branches and work through them for parallel-style development.
alwaysApply: false
---

## Parallel work with `cuttings`

This project uses the [`cuttings`](https://github.com/ChristophBe/cuttings)
CLI to give each independent branch its own isolated git worktree
(`.worktrees/<branch>/` by default). When asked to split work across several
independent branches:

1. Confirm the CLI is installed: `command -v cuttings`. If it isn't,
   say so rather than falling back to raw `git worktree` commands — using
   the real CLI keeps `cuttings list`/`shell`/`remove` usable afterward.
2. `cuttings new <branch>` normally opens an interactive shell and would
   hang a non-interactive session. Create the worktree without it by
   pointing `$SHELL` at the `true` binary so the command does its real work
   (branch/config handling, `git worktree add ...`) and then exits
   immediately instead of blocking:

   ```bash
   SHELL="$(type -P true)" cuttings new <branch> [--from <base-ref>]
   ```

   (`command -v true` can resolve to a shell builtin with no filesystem
   path, which breaks this trick — `type -P true` forces an on-disk path.)
   If the cutting already exists this command errors out; get its path
   from `cuttings list` instead.
3. Work through each created cutting directory in turn — `cd` into it
   before making changes there.
4. `cuttings list` shows every active cutting and its path.
   `cuttings remove <branch>` deletes one once its work is merged or
   abandoned (the branch itself is preserved); it refuses worktrees with
   uncommitted changes, so surface that rather than discarding changes.
