name: Unit Tests
description: Run unit tests with coverage, and upload the coverage artifact
runs:
  using: composite
  steps:
    - run: make test
      shell: bash
    - uses: actions/upload-artifact@v7
      with:
        name: coverage
        path: coverage.out
